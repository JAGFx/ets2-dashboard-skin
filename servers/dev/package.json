{
	"name": "dev",
	"version": "1.0.0",
	"description": "",
	"main": "src/index.ts",
	"type": "module",
	"scripts": {
		"build": "npx tsc -p tsconfig.json",
		"watch": "npx concurrently --kill-others --names 'BUILD,PATH,RUN' -c 'tsc -w' 'tsc-alias -w' 'nodemon --experimental-modules -q -w dist dist/index.js'",
		"start": "npm run build && node index.js"
	},
	"author": "Emmanuel SMITH <hey@emmanuel-smith.me>",
	"license": "MIT",
	"dependencies": {
		"ets2-dashboard-core": "file:../../lib/jagfx/core",
		"ets2-dashboard-server": "file:../../lib/jagfx/server",
		"fastify": "^4.9.2",
		"socket.io": "^4.1.3"
	},
	"devDependencies": {
		"@types/node": "^18.11.9",
		"concurrently": "^7.5.0",
		"nodemon": "^2.0.20",
		"tsc-alias": "^1.7.1",
		"typescript": "^4.8.4"
	}
}
