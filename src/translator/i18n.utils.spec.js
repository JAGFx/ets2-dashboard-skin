import { fallbackLocale } from '@/translator/i18n.util';

describe('Incremental Search Actions', () => {
  it('Should create an incremental search action', () => {
    expect(1 + 2).toBe(3);
  });
});
